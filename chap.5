1.关键是了解pointer,array和reference声明的阅读顺序.(那么,书写顺序呢?)
char* p;
int a[10] a;
int[10]& p;
string[]* p;
char** p;
const int i;
const int* p;
int *const p;

温馨提示:把语句翻译为英文后,再把声明写出来就容易多了.
T* p: p is a point to T.
T a[size]: a is an array of size elements of type T.
X& r: r is a reference to X.

2.It depends(:

3.
typedef int* int_ptr;
typedef char** ;

4.关键是了解pointer和reference的联系.由这道题目引出的问题:指针和数组有什么关系?它们各自是怎么实现的?
void swap(int* a, int* b){
    int temp = *a;
    *a = *b;
    *b = temp;
}

void swap(int& a, int& b){
    int temp = a;
    a = b;
    b = temp;
}

5.
